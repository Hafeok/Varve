<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- One source. A package that is not on nuget.org does not enter this
       repository without an ADR that says where it comes from. -->
  <packageSources>
    <clear />
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" protocolVersion="3" />
  </packageSources>

  <!-- Source mapping is on so that adding a second source later cannot
       silently redirect an existing package id. -->
  <packageSourceMapping>
    <packageSource key="nuget.org">
      <package pattern="*" />
    </packageSource>
  </packageSourceMapping>

  <packageRestore>
    <add key="enabled" value="True" />
    <add key="automatic" value="True" />
  </packageRestore>
</configuration>
