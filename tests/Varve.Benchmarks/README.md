# Varve.Benchmarks

Built in CI, never run there, and not a gate. ADR 0027: a benchmark that
gates turns a noisy measurement into a flaky build, and a shared runner is the
noisiest machine there is. Numbers are taken deliberately, on a stated
machine, and reported with the machine. The `build-benchmarks` job only
compiles the project, so that a change elsewhere cannot break it unseen, as
one did in 5c. The project is a layer 6 host (ADR 0060).

```bash
dotnet run -c Release --project tests/Varve.Benchmarks -- --filter '*'
```

## Milestone 3a — reading 100,000 N-Quads

**Machine.** Intel Xeon @ 2.80 GHz, 4 logical and 4 physical cores, 15 GiB RAM,
Ubuntu 24.04.4 LTS (kernel 6.18.44), a cloud container rather than dedicated
hardware. .NET SDK 10.0.401, runtime 10.0.12, X64 RyuJIT `x86-64-v4`.
BenchmarkDotNet 0.15.8, `--warmupCount 3 --iterationCount 8 --invocationCount 1
--unrollFactor 1`. Baseline: dotNetRDF (`dotNetRdf.Core` 3.5.2).

**Dataset.** Generated by `Dataset.cs`, so the run reproduces without a file:
100,000 quads across five named graphs, seventeen predicates, one subject in
eight a blank node, and objects mixed between IRIs, plain literals, typed
literals, language-tagged literals and literals carrying escapes. Deliberately
not a nice document — a parser that is fast only on IRIs is fast on the easy
half of the problem. It is still synthetic, and a real dataset has a different
term-length distribution, escape density and IRI-prefix profile.

| | Mean | Quads/s | Allocated | vs. dotNetRDF |
|---|---:|---:|---:|---:|
| Varve — views | 148.5 ms ± 1.8 | 673,000 | **856 B** | 19.0× faster |
| Varve — owned terms | 167.0 ms ± 5.1 | 599,000 | 24.5 MB | 16.9× faster |
| Varve — into `InMemoryDataset` | 401.4 ms ± 5.5 | 249,000 | 90.2 MB | 7.0× faster, 7.3× less memory |
| dotNetRDF — `TripleStore` | 2,826.5 ms ± 53.6 | 35,000 | 660.7 MB | — |

**Read the third row, not the first, for the comparison.** The first row is not
doing dotNetRDF's work: it hands out views over the parser's own buffer and
owns nothing, which is the point of the streaming path and is not a graph
anybody can query afterwards. The third row builds a queryable dataset with
interned terms, which is the like-for-like run, and it is **7.0× faster and
uses 7.3× less memory**. The 19× headline is real but it is measuring a
different thing, and quoting it without this paragraph would be dishonest.

**856 bytes for 100,000 quads** is one `TermArena` — the same figure
`AllocationTests` reports as the fixed cost of a parse, arrived at
independently. The allocation test asserts zero *per quad*; this is what that
looks like at a hundred thousand.

**What is not measured.** Writing, the pull reader, the streaming entry points,
`ReadOnlySequence` fragmentation, and anything at all about query. Parsing is
the only thing milestone 3a ships, so it is the only thing benchmarked.

## Milestone 3b — reading 100,000 quads of Turtle

**Machine.** Intel Xeon @ 2.10 GHz, 4 logical and 4 physical cores, Ubuntu
24.04.4 LTS (Noble Numbat), a cloud container rather than dedicated hardware.
.NET SDK 10.0.401, runtime 10.0.12, X64 RyuJIT `x86-64-v4`. BenchmarkDotNet
0.15.8, default job. Baseline: dotNetRDF (`dotNetRdf.Core` 3.5.2).

**A different machine from the 3a section above** — 2.10 GHz here, 2.80 GHz
there — so the two sets of absolute numbers are not comparable with each other.
The ratios within each table are.

**Dataset.** Generated by `TurtleDataset.cs`. Every statement carries a
prefixed name to expand, a predicate-object list, an object list, a blank node
property list and a two-item collection, so the measurement is of what Turtle
adds over N-Quads rather than of line splitting.

```
$ dotnet run -c Release --project tests/Varve.Benchmarks -- --datasets
N-Quads: 15,644,119 bytes, 100,000 quads, 156.4 bytes/quad
Turtle:  1,106,705 bytes, 100,000 quads,  11.1 bytes/quad
```

**Do not read quads per second across the two sections.** Both documents carry
100,000 quads and the Turtle one is **fourteen times smaller**, because a
collection's four `rdf:first`/`rdf:rest` quads cost almost no bytes to write
down. Per quad Turtle therefore looks far faster than N-Quads; per byte it is
slower, which is the honest direction — it is doing more grammar. The quad
count is checked at `[GlobalSetup]` rather than asserted here, so a parser that
quietly stopped halfway could not produce a flattering number.

| | Mean | Quads/s | Allocated | vs. dotNetRDF |
|---|---:|---:|---:|---:|
| Varve — views | 16.7 ms ± 0.6 | 6,000,000 | **6.1 KB** | 44.4× faster |
| Varve — owned terms | 28.8 ms ± 0.6 | 3,470,000 | 37.2 MB | 25.8× faster |
| Varve — into `InMemoryDataset` | 100.2 ms ± 2.0 | 998,000 | 54.8 MB | **7.4× faster, 5.5× less memory** |
| Varve — read and write back | 38.4 ms ± 0.8 | 2,600,000 | 15.0 MB | — |
| dotNetRDF — `Graph` | 742.0 ms ± 14.5 | 135,000 | 301.2 MB | — |

**Read the third row, not the first**, for the same reason as milestone 3a: the
streaming rows hand out views over the parser's own buffer and own nothing,
which is not a graph anybody can query afterwards. The like-for-like run builds
a queryable dataset with interned terms, and it is **7.4× faster and uses 5.5×
less memory**. The 44× headline is real and it is measuring a different thing.

The **fourth row has no baseline on purpose**: it reads the document and writes
Turtle back out, and dotNetRDF's writer was not measured, so there is nothing
honest to divide by. It is here because a round trip is what the fixed-point
property test exercises 569 times, and this says what it costs.

**6.1 KB for 100,000 quads** is the parse's fixed cost — the arena and the
prefix table — and it does not grow with the document. `AllocationTests`
asserts zero bytes *per quad* on the same reader; this is what that looks like
at a hundred thousand.

**What is not measured.** The pull reader, the streaming entry points,
`ReadOnlySequence` fragmentation, error recovery, and query. A document whose
statements are one triple each would move every row, because the per-statement
costs — the arena reset, the pending buffer — would then be paid per quad.

## Milestone 4 — the in-memory store

**Machine.** Intel Xeon @ 2.10 GHz, 4 logical and 4 physical cores, 15 GiB RAM,
260 MiB L3, Ubuntu 24.04.4 LTS, a cloud container rather than dedicated
hardware. .NET SDK 10.0.401, runtime 10.0.12, X64 RyuJIT `x86-64-v4`.
BenchmarkDotNet 0.15.8. No baseline: dotNetRDF's in-memory store has no log,
no effective deltas and no as-of reads, so a comparison would time different
work. Oxigraph remains owed (ADR 0027).

```bash
dotnet run -c Release --project tests/Varve.Benchmarks -- --store-sizes
dotnet run -c Release --project tests/Varve.Benchmarks -- --filter '*CommitBenchmarks*' \
  --warmupCount 2 --iterationCount 8 --invocationCount 1 --unrollFactor 1
dotnet run -c Release --project tests/Varve.Benchmarks -- --filter '*ScanBenchmarks*' \
  --warmupCount 3 --iterationCount 10
```

**Dataset.** Generated by `StoreDataset` in `StoreBenchmarks.cs`: 1,000,000
quads, 100,000 subjects of ten quads, seventeen predicates, a sixth of the quads
in the default graph and the rest across five named graphs; objects are shared
IRIs, canonical integers (inline ids, no dictionary entry), plain and
language-tagged literals, and non-canonical integers (dictionary entries).
769,188 distinct terms take a dictionary entry. Loaded as 100 commits of 10,000.

### Size

| | Bytes | Per quad |
|---|---:|---:|
| The six key arrays of the default projection (ADR 0041) | 192,000,000 | **192.0** |
| A checkpoint: the six arrays, the dictionary, a header | 228,971,219 | 229.0 |
| The log: 100 commits, records, headers and chain | 68,989,323 | 69.0 |
| Managed heap growth for the whole store (log, dictionary, runs, one checkpoint) | 579,084,176 | 579.1 |

### Commits

| | Mean | Throughput | Allocated |
|---|---:|---:|---:|
| 100,000 quads in one commit, into an empty dataset | 175.2 ms ± 8.9 | 571,000 quads/s | 160.9 MB |
| 100,000 quads as 100 commits of 1,000 | 260.0 ms ± 7.9 | 385,000 quads/s | 272.9 MB |
| 1,000 single-quad commits of new terms, into 100,000 quads | 6.6 ms ± 0.7 | 150,000 commits/s | 6.4 KB per commit |

A commit includes normalisation against the head, the dictionary, SHA-256 of
the body and the header, the append, and the default projection's update —
everything T1 does. The memory backend's durability is `None`, so no flush
costs anything; a file backend's `fsync` will dominate small commits.

### Scans, over a pinned read of 1,000,000 quads

| | Mean | Quads | Throughput | Allocated |
|---|---:|---:|---:|---:|
| Every quad, every graph | 22.1 ms ± 1.2 | 1,000,000 | 45 M quads/s | 320 B |
| One predicate of seventeen (a POSG range) | 1.39 ms ± 0.11 | 58,824 | 42 M quads/s | 320 B |
| The default graph (a graph-first range) | 4.47 ms ± 0.26 | 166,667 | 37 M quads/s | 320 B |
| 10,000 subject lookups (SPOG ranges) | 7.8 ms ± 0.6 | 100,000 | 0.78 µs per lookup | 320 B per lookup |

320 bytes is one cursor and its stream array, per scan, whatever the scan
returns: the scan itself allocates nothing per quad, which `AllocationTests`
asserts and this shows at a million.

### Against ADR 0012's revisit condition

ADR 0012 is to be superseded if "milestone 4 benchmarks of index size and scan
throughput contradict the choice" of 64-bit counter-allocated ids. **They do
not.** Index size is 192 bytes per quad for six orders; the content-derived
128-bit ids the ADR rejected would make the same layout 384. Scan throughput is
37–45 M quads/s single-threaded, allocation-free, over merged runs. Neither
number argues for a wider id. What the benchmark cannot yet test is the ADR's
locality and compression hypothesis on disk, which is milestone 6's layout.

## Milestone 5a — parsing the SPARQL syntax corpus

**Machine.** Intel Xeon @ 2.80 GHz, 4 logical and 4 physical cores, 15 GiB,
Ubuntu 24.04.4 LTS (Noble Numbat), kernel 6.18, a cloud container rather than
dedicated hardware. .NET SDK 10.0.401, runtime 10.0.12, X64 RyuJIT
`x86-64-v4`. BenchmarkDotNet 0.15.8, default job. Baseline: dotNetRDF
(`dotNetRdf.Core` 3.5.2, `SparqlQueryParser` and `SparqlUpdateParser`).

The same clock as the milestone 3a section and a different one from 3b and
4; as before, absolute numbers are comparable only within a section and the
ratios are comparable everywhere.

**Corpus.** Loaded by `SparqlCorpus` in `SparqlBenchmarks.cs` from the W3C
submodule, and printed rather than asserted in prose:

```
$ dotnet run -c Release --project tests/Varve.Benchmarks -- --sparql-corpus
1.0 and 1.1 corpus: 215 queries both parsers accept (0 that dotNetRDF rejects left out), 15,415 bytes
1.2 corpus: 120 cases, 13,386 bytes
large update: 12,910 bytes
```

The first corpus is every positive *query* case of the SPARQL 1.0 and 1.1
syntax suites, parsed at version 1.1 by both parsers, with a case left out if
dotNetRDF refuses it so that the two rows time the same work. None were left
out. The second is every positive case of the six SPARQL 1.2 syntax suites,
queries and updates together; dotNetRDF has no SPARQL 1.2, so that row has no
baseline. The third is `syntax-update-2/large-request-01.ru`, an `INSERT DATA`
of 868 quads, the one input in the suites that is big enough to measure by
itself. Each row parses its whole corpus once, so a mean is the cost of the
corpus and not of a query; the per-query figures below are that mean divided
by the count.

| | Mean | Per case | Bytes/s | Allocated | vs. dotNetRDF |
|---|---:|---:|---:|---:|---:|
| Varve — 1.0 and 1.1 corpus, 215 queries | 761 µs ± 26 | 3.5 µs | 20.3 MB/s | 304 KB | **8.0× faster, 13.4× less memory** |
| dotNetRDF — 1.0 and 1.1 corpus, 215 queries | 6,065 µs ± 115 | 28.2 µs | 2.5 MB/s | 4,066 KB | — |
| Varve — the same, parse and write back | 866 µs ± 28 | 4.0 µs | — | 304 KB | — |
| Varve — 1.2 corpus, 120 cases | 578 µs ± 15 | 4.8 µs | 23.2 MB/s | 324 KB | no baseline |
| Varve — large update, 868 quads | 985 µs ± 29 | 1.1 µs per quad | 13.1 MB/s | 474 KB | **4.5× faster, 5.1× less memory** |
| dotNetRDF — large update, 868 quads | 4,441 µs ± 69 | 5.1 µs per quad | 2.9 MB/s | 2,442 KB | — |

**The corpus is small, and that is the caveat to read first.** Fifteen
kilobytes of queries is the whole positive syntax corpus, and it fits in the
L2 cache; a workload of long queries with long literals would move every row,
and there is no such corpus in the suites. What the table measures is the
parser's dispatch across every construct of the grammar on realistic query
sizes, which is the shape a query engine sees, and not bulk throughput, which
is what the Turtle sections measure.

**The allocation is the tree.** 304 KB over 215 queries is 1.4 KB per query,
and it is all algebra: immutable records with their lists and the interned
terms, which is what the parse returns. `AllocationTests` asserts that parsing
350 more triple patterns allocates exactly what building them by hand does,
so the parser's own working set — its token buffer, pooled lists and the
interning table — is rented and returned and shows here as nothing. The
milestone 3b reader's 6.1 KB fixed cost is the same idea; the difference is
that a parse's result here is a tree the caller owns.

**The third row has no baseline on purpose**, as at milestone 3b: it parses,
serialises the algebra back to text and discards the text, and dotNetRDF's
formatter was not measured. It says that writing costs about a seventh of
parsing, with no allocation beyond the tree, because the output buffer is
rented once at set-up. It is the property the round-trip test exercises 3,000
times per run.

**The large update is slower per byte than the small queries**, 13 MB/s
against 20, and that is the direction to expect: the queries are mostly
keywords and short names, while the update is 868 terms each resolved against
`BASE`, validated as an IRI and interned, then held in a `QuadPattern` record
apiece. 474 KB for 868 quads is 560 bytes per quad, which is the record, its
four terms and the `RdfTerm` behind each, and the ratio against dotNetRDF is
narrower here because its cost per term is also mostly the term.

**What is not measured.** Error paths, UTF-16 input (transcoded once at the
entry, at `Utf8.FromUtf16` speed), the rewriter, and anything after parsing;
nothing evaluates a query yet. The 1.2 row will get a baseline when another
.NET parser reads SPARQL 1.2. ADR 0050's evaluator benchmark, the one with a
revisit condition attached, is milestone 5b's and needs the evaluator first.

## Milestone 5b — evaluation: ADR 0050's arms, a BSBM-style mix, Oxigraph

**Machine.** Intel Xeon @ 2.80 GHz, 4 logical and 4 physical cores, 15 GiB,
Ubuntu 24.04.4 LTS, kernel 6.18, a cloud container rather than dedicated
hardware. .NET SDK 10.0.401, runtime 10.0.12, X64 RyuJIT `x86-64-v4`.
BenchmarkDotNet 0.15.8. Baselines: dotNetRDF (`dotNetRdf.Core` 3.5.2, the
Leviathan engine over its in-memory `TripleStore`) and pyoxigraph 0.5.11 —
**Oxigraph through its Python binding**, not the Rust library. The same clock
as the 3a and 5a sections.

### ADR 0050 — the three arms, and ADR 0022's revisit condition

**The suites' wall time** (`--suite-time 20`, run three times): the 522 cases
of the SPARQL 1.0 and 1.1 evaluation suites that are not blocked, over the
store, each case loading its data, parsing its query, evaluating and
comparing; three warm-up rounds, then the arms interleaved run by run. Every
case must pass in every arm or the run stops.

| Arm | Median, three processes | Range within a process |
|---|---:|---:|
| `InlineAccessor` | 79.0, 76.0, 76.7 ms | 62–284 ms |
| `Externalise` | 79.8, 75.3, 76.9 ms | 62–223 ms |
| `Materialise` | 75.2, 78.2, 74.6 ms | 64–230 ms |

**Indistinguishable**, and that is the finding: the suites' data are a few
dozen triples a case, so the time is loading and parsing, not value access.
The first run interleaved nothing and the first arm read 2.5× slower — it was
paying for tiered compilation — which is why the arms now alternate.

**A million inline integers** (`FilterBenchmarks`): one million quads
`ex:sN ex:v N` committed to the store in ten commits, read through a pinned
view; `--warmupCount 2 --iterationCount 8 --invocationCount 1`.

| Case | Solutions | Accessor | Externalise | Materialise | Accessor allocates |
|---|---:|---:|---:|---:|---:|
| `FILTER(?o > 990000)` | 9,999 | **141 ms** | 312 ms | 3,287 ms | 45.8 MB |
| `FILTER(?o > 500000)` | 499,999 | **164 ms** | 322 ms | 3,338 ms | 45.8 MB |
| `FILTER(?o > 10000)` | 989,999 | **159 ms** | 326 ms | 3,416 ms | 45.8 MB |
| `FILTER(?o = "5"^^xsd:integer)` | 1 | **136 ms** | 323 ms | 3,246 ms | 45.8 MB |
| `ORDER BY ?o` | 1,000,000 | **785 ms** | 3,944 ms | 4,958 ms | 180.8 MB |

The externalise arm allocates 343 MB on the filters and the materialised arm
692 MB. **The accessor arm's 45.8 MB is the basic graph pattern's rows**, 48
bytes for each of the million matches the filter then looks at, which is §11's
row per solution and not a cost per quad; it is why the four filters cost the
same whatever they keep. Moving a filter into the scan so that a rejected match
costs no row is the obvious next step and is not taken here.

**Verdict: ADR 0022's revisit condition does not fire.** It fires only if the
materialised arm beats the accessor arm on both measurements by more than
noise; it ties on the first and loses the second by 20–23× on filters and
6.3× on the sort. The materialised arm still joins on handles, so its cost is
a lower bound on a term-based contract's. The externalise arm — ADR 0022's
contract as shipped — says what the accessor bought: 2.0–2.3× on filters,
5.0× on the sort. ADR 0022 carries the dated verdict.

**The benchmark found two defects on its first run**, both fixed before the
numbers above: the materialised arm failed 25 suite cases (a join through a
blank node, whose label the store does not internalise back), and `ORDER BY`
externalised both keys on every comparison — 5.7 s and **10.2 GB** for the
sort in the accessor arm, now 785 ms and 181 MB. A conformance guard now runs
every case in every arm.

### A BSBM-style mix, against dotNetRDF and Oxigraph

**Dataset.** Generated by `Bsbm.cs` from a fixed seed, in the Berlin SPARQL
Benchmark's explore schema: 2,000 products with types, eight features each,
producers and three numeric and two textual properties; ten offers per
product with a decimal price, a vendor, a date and delivery days; five reviews
per product with dates and optional integer ratings; **202,373 triples**,
38.7 MB of N-Triples. **Queries**: BSBM explore's shapes with fixed
parameters — Q1 type, feature and a numeric filter; Q2 one product's details
with an `OPTIONAL`; Q3 negation by `OPTIONAL`/`!bound`; Q4 a `UNION`; Q5
similar products, a self-join on features with range filters; Q7 offers and
reviews under nested `OPTIONAL`s; Q8 recent reviews by date; Q10 cheap US
offers ordered by price; and Qa, an aggregate over every review. Not BSBM
itself, whose generator is a Java tool with parameters drawn per run. **Every
engine gives the same row count on every query**, checked at setup, and
pyoxigraph's agrees too.

| Query | Varve, store | dotNetRDF | vs. dotNetRDF | Allocated, vs. dotNetRDF | pyoxigraph |
|---|---:|---:|---:|---:|---:|
| Q1 | **250 µs** | 1,238 µs | 5.0× | 86 KB, 8.4× less | 624 µs |
| Q2 | **24 µs** | 648 µs | 27× | 20 KB, 12.5× less | 179 µs |
| Q3 | **308 µs** | 2,045 µs | 6.6× | 120 KB, 6.8× less | 766 µs |
| Q4 | **496 µs** | 4,310 µs | 8.7× | 175 KB, 13.3× less | 1,211 µs |
| Q5 | **6.7 ms** | 79.5 ms | 11.9× | 1.8 MB, 10.7× less | 15.6 ms |
| Q7 | **43 µs** | 786 µs | 18× | 35 KB, 8.4× less | 251 µs |
| Q8 | **45 µs** | 440 µs | 9.7× | 24 KB, 8.1× less | 168 µs |
| Q10 | **6.8 ms** | 102.2 ms | 15.0× | 1.8 MB, 12.5× less | 7.7 ms |
| Qa | **9.4 ms** | 142.4 ms | 15.2× | 3.3 MB, 12.3× less | 34.0 ms |

**Read the dotNetRDF column as the like-for-like one.** Both are .NET engines
over an in-memory indexed store, timed by BenchmarkDotNet in one process, and
Varve's store is 5–27× faster with 7–13× less allocation.

**Read the pyoxigraph column with its caveat.** It is the median of 30 runs
after 5 warm-up runs, timed with `perf_counter` inside the Python call around
the query and the iteration of its results (`oxigraph/bsbm.py`), in-memory
store loaded beforehand: it excludes interpreter start-up and loading, and it
includes the binding's cost of building a Python object per solution, which
the Rust library does not pay. It is the comparison asked for, stated as what
it is; a comparison with Oxigraph proper needs a Rust harness, and ADR 0027
leaves that owed. On these terms Varve is faster on every query, by 1.1× on
Q10, where both spend their time in the price sort, to 7× on Q2.

**The third engine, `InMemoryDataset`, is not in the table because it is not
a query engine.** Its `Match` and `Estimate` are linear scans over every quad
by design — it holds a parsed file for layer 3 to run against, and says so —
so each triple pattern of these queries reads all 202,373 triples: 27 ms for
Q2 and 11.6 s for Qa. The evaluator is the same; the row measures the source.

**Q10 was first written with a product constant**, returned no rows in any
engine, and was widened to every product's offers before these numbers were
taken: a benchmark whose answer is empty checks nothing.

### The differential run against Oxigraph

Every evaluation case that is not blocked and calls no `SERVICE`, 537, was
answered by pyoxigraph from the same files and compared with Varve's answer
by the conformance comparator:

```
$ dotnet run -c Release --project tests/Varve.Benchmarks -- --export-cases cases.json
$ python tests/Varve.Benchmarks/oxigraph/differential.py cases.json answers/
pyoxigraph 0.5.11: 537 cases, 2 refused
$ dotnet run -c Release --project tests/Varve.Benchmarks -- --differential answers/
508 agree, 27 disagree, 2 refused by Oxigraph
```

Varve passes all 537, so each disagreement is Oxigraph departing from the
suite's expected answer. They fall into seven behaviours — typed literals
stored by value, `GRAPH ?g` evaluated with `?g` pre-bound, double braces
scoped as one group, a zero-length path from a constant not in the graph,
`GROUP_CONCAT`'s language tag, `BNODE(str)` across solutions, and a date
order — listed case by case in `docs/spec/sparql-evaluation.md` §13.4.

## Milestone 5c — updates and RDFC-1.0, against dotNetRDF and Oxigraph

**Machine.** Intel Xeon @ 2.10 GHz, 4 logical and 4 physical cores, 15 GiB,
Ubuntu 24.04.4 LTS, kernel 6.18.44, a cloud container rather than dedicated
hardware. .NET SDK 10.0.401, runtime 10.0.12, X64 RyuJIT `x86-64-v4`.
BenchmarkDotNet 0.15.8. Baselines: dotNetRDF (`dotNetRdf.Core` 3.5.2, the
Leviathan update processor over its `TripleStore`, and its `RdfCanonicalizer`)
and pyoxigraph 0.5.11 — **Oxigraph through its Python binding**, not the Rust
library. The 2.10 GHz clock of the 3b and 4 sections, not the 2.80 GHz one of
3a, 5a and 5b.

```bash
dotnet run -c Release --project tests/Varve.Benchmarks -- --update-export update/
python tests/Varve.Benchmarks/oxigraph/update.py update/
dotnet run -c Release --project tests/Varve.Benchmarks -- --filter '*.UpdateBenchmarks*'
dotnet run -c Release --project tests/Varve.Benchmarks -- --filter '*DeleteInsertWhereBenchmarks*'
dotnet run -c Release --project tests/Varve.Benchmarks -- --filter '*CanonicaliseBenchmarks*'
```

The engines read the same bytes: `--update-export` writes the requests, the
store and the graphs that the .NET benchmarks build in memory. The pyoxigraph
column is timed with `perf_counter` inside one Python process (see the
caveat under 5b; it includes the binding's cost, excludes start-up).

### INSERT DATA of N quads, into an empty store

The first N quads of `StoreDataset` as one `INSERT DATA` request — plain
triples and `GRAPH` blocks across five named graphs, 15.5 MB of text for
100,000. **Parsing is inside the time for every engine**, because
pyoxigraph's `update` takes text. One invocation an iteration, each into an
empty store made in the untimed setup; the quad count is checked after every
invocation. Median; BenchmarkDotNet `--warmupCount 2 --iterationCount 8`.

| N | Varve | dotNetRDF | pyoxigraph | Varve allocates |
|---:|---:|---:|---:|---:|
| 10,000 | 68 ms | 104 ms | **30 ms** | 36.4 MB, 2.4× less than dotNetRDF |
| 100,000 | 475 ms | 2,078 ms | **392 ms** | 340 MB, 2.5× less |

**pyoxigraph is faster, 1.2× at 100,000 and 2.2× at 10,000, and that is the
first row to read.** Varve is 4.4× faster than dotNetRDF at 100,000. The
10,000 row is noisy in both .NET engines (a standard deviation of half the
mean in Varve's), which is garbage collection in one-invocation iterations;
the medians are quoted for that reason.

Where Varve's 475 ms goes: **143 ms is parsing** (a `VarveParseOnly` row in
the same run, 98 MB), about 175 ms is the store committing 100,000 quads in
one commit (milestone 4's figure, on this clock), and the remaining ~155 ms is
the executor — staging every term, the asserted set, sorting the delta and
building the request. That last part is the obvious place to look first; it
was not optimised in this milestone.

### DELETE/INSERT WHERE over a million-quad store

`StoreDataset`'s 1,000,000 quads, loaded once. The request moves every quad of
one predicate in the named graphs to another predicate — **49,020 quads
deleted and 49,020 inserted** — and the next request moves them back, so each
invocation does the same work over what the last one left. Two invocations
an iteration, `--warmupCount 2 --iterationCount 8`; pyoxigraph the median of
16 requests after 4 warm-up requests. After the run each engine is checked
to hold the 49,020 quads where they started. Varve's store grows a commit per
request, which is what a store under updates does.

| | Mean | Allocated | vs. dotNetRDF |
|---|---:|---:|---:|
| Varve | **186 ms** ± 18 | 125 MB | 9.4× faster, 2.4× less memory |
| pyoxigraph | 205 ms (median) | — | 8.5× faster |
| dotNetRDF | 1,747 ms ± 43 | 302 MB | — |

**Varve and pyoxigraph are within 10% of each other** here, Varve ahead; the
noise on a shared container is about that size, so read it as a tie.

### RDFC-1.0 of a 100,000-triple graph

`CanonGraph`: 12,500 subjects of eight triples — a type, a label, a number,
and five links to other subjects by strides that make one connected
structure. Three shapes: **Blank**, every subject a blank node and each told
apart at first degree; **Twins**, the same with 250 pairs sharing their label
and number, so each pair needs the N-degree step; **Blank1000**, only the
first 1,000 subjects blank and the rest IRIs. SHA-256, to the canonical
N-Quads document, which every engine produces and which pyoxigraph's run
serialises inside its time. **The three engines' documents are byte-identical
on every shape each of them completes** — checked by the benchmark's setup
against dotNetRDF and by `update.py` against Oxigraph.

| Shape | Varve | dotNetRDF | pyoxigraph | Varve allocates |
|---|---:|---:|---:|---:|
| Blank | **255 ms** ± 14 | refused | 485 ms | 178 MB |
| Twins | **270 ms** ± 15 | refused | 523 ms | 200 MB |
| Blank1000 | **187 ms** ± 6 | 1,525 ms ± 113 | 337 ms | 119 MB, 7.1× less than dotNetRDF |

**dotNetRDF 3.5.2 refuses any dataset of more than 1,000 blank nodes** with
"Recursion limit reached" from its N-degree hash — measured, not inferred: a
chain or a set of 1,000 unlinked blank nodes with distinct literals
canonicalises, 1,001 does not, although RDFC-1.0 never reaches the N-degree
step for a blank node with a unique first-degree hash. Blank1000 exists to
give it a like-for-like row, where Varve is 8.1× faster.

Varve is **1.8–1.9× faster than pyoxigraph** on every shape. The twins cost
Varve 6% over the Blank shape; the N-degree step for 500 blank nodes is cheap
next to hashing 100,000 quads. The work limit (ADR 0059) is a multiple of the
blank nodes without a unique first-degree hash, default 1,000; **the Twins
shape needs between 21 and 25** — it stops at a limit of 20 and completes at
25 — so the default leaves a margin of 40× on a graph of this kind.

**What is not measured.** The file backend (milestone 6), updates with
`LOAD`, conflict retries, SHA-384, and canonicalisation of adversarial
graphs — the rdf-canon suite's poison graphs are what the work limit is for,
and a benchmark of a refusal measures nothing.
