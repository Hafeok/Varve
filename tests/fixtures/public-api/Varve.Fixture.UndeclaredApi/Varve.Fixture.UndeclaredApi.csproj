<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Packable, so PublicApiAnalyzers applies and the two baseline files
         beside this project are picked up as AdditionalFiles. The project
         exists to fail. -->
    <IsPackable>true</IsPackable>
    <ArchLayer>0</ArchLayer>
  </PropertyGroup>
</Project>
