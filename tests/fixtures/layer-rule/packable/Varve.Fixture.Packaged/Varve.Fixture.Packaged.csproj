<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Packable and layered: the ordinary shape of a published package. It
         also exercises the packable block in Directory.Build.targets, which
         nothing else in the repository does yet. -->
    <IsPackable>true</IsPackable>
    <ArchLayer>0</ArchLayer>
  </PropertyGroup>
</Project>
