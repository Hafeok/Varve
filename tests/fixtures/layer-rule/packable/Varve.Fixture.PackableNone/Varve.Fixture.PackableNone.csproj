<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Packable and declaring no layer. Being packed puts an assembly in the
         package graph whatever it is called, so VARVE0005 refuses it (ADR
         0064). Meant to fail. -->
    <IsPackable>true</IsPackable>
  </PropertyGroup>
</Project>
