<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Packable, so BannedApiAnalyzers and eng/BannedSymbols.txt apply. The
         project exists to fail. -->
    <IsPackable>true</IsPackable>
    <VarveLayer>0</VarveLayer>
  </PropertyGroup>
</Project>
