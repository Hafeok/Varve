namespace Varve.Fixture.LicenceHeader;

// The violating sample: no MPL-2.0 notice, deliberately. The gate must reject
// this one, and the repository-wide run must not see it.
internal sealed class WithoutNotice
{
}
