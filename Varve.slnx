<Solution>
  <!-- tests/fixtures/ is deliberately absent. Those projects exist to fail to
       build; a solution member that fails to build would fail CI. They are
       driven directly by Varve.Analyzers.Tests. -->
</Solution>
